<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hanna.wx.db.dao.WxUserManagerDao">
	<!-- 根据ID获取微信用户信息 -->
	<select id="getWxUserInfoById" parameterType="string" resultType="WxUserInfo" >
		SELECT 
		ID id,
		OPENID openid,
		NICKNAME nickname,
		SUBSCRIBE subscribe,
		SUBSCRIBE_TIME subscribe_time,
		SEX sex,
		COUNTRY country,
		PROVINCE province,
		CITY city,
		LANGUAGE language,
		HEADIMGURL headimgurl,
		UNIONID unionid,
		REMARK remark,
		GROUPID groupid
		FROM WX_USER_INFO
		WHERE ID = #{id}
	</select>
	
	<!-- 查询微信用户列表 -->
	<select id="queryWxUser" parameterType="SysSearchDto" resultType="WxUserInfo" >
		SELECT 
		ID id,
		OPENID openid,
		NICKNAME nickname,
		SUBSCRIBE subscribe,
		SUBSCRIBE_TIME subscribe_time,
		SEX sex,
		COUNTRY country,
		PROVINCE province,
		CITY city,
		LANGUAGE language,
		HEADIMGURL headimgurl,
		UNIONID unionid,
		REMARK remark,
		GROUPID groupid
		FROM WX_USER_INFO
		WHERE 1 = 1
		
		<if test="openid != null and openid != ''">
			AND OPENID = #{openid}
		</if>
		
		<if test="startDate != null and startDate != ''">
			<![CDATA[
				AND date_format(SUBSCRIBE_TIME, '%Y-%m-%d') >= str_to_date(#{startDate},'%Y-%m-%d') 
			]]>
		</if>
		
		<if test="endDate != null and endDate != ''">
			<![CDATA[
				AND date_format(SUBSCRIBE_TIME, '%Y-%m-%d') <= str_to_date(#{endDate},'%Y-%m-%d')
			]]>
		</if>
		
		LIMIT #{begin},#{rows}
	</select>
	
	<!-- 查询微信用户列表数 -->
	<select id="countWxUser" parameterType="SysSearchDto" resultType="int" >
		SELECT COUNT(ID) 
		FROM WX_USER_INFO
		WHERE 1 = 1
		
		<if test="openid != null and openid != ''">
			AND OPENID = #{openid}
		</if>
		
		<if test="startDate != null and startDate != ''">
			<![CDATA[
				AND date_format(SUBSCRIBE_TIME, '%Y-%m-%d') >= str_to_date(#{startDate},'%Y-%m-%d') 
			]]>
		</if>
		
		<if test="endDate != null and endDate != ''">
			<![CDATA[
				AND date_format(SUBSCRIBE_TIME, '%Y-%m-%d') <= str_to_date(#{endDate},'%Y-%m-%d')
			]]>
		</if>
	</select>
</mapper>